{
  "name": "pernet-paysages",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-site.mjs",
    "check": "node scripts/check-site.mjs",
    "dev": "npm run generate && vite",
    "build": "npm run generate && vite build && npm run check",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
